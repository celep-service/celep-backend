server:
  port: 8080

spring:
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show_sql: true
        format_sql: true
        use_sql_comments: true
  http:
    encoding:
      charset: UTF-8
      enabled: true
      force: true
  server:
    tomcat:
      uri-encoding: UTF-8


  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: admin
    password: admincody
    url: jdbc:mysql://cody.cf54bs5i8vlt.ap-northeast-2.rds.amazonaws.com:3306/celep?useUnicode=true&characterEncoding=utf8&serverTimezone=UTC
    hikari:
      max-lifetime: 25800 # Possibly consider using a shorter maxLifetime value. 오류로 인해 추가

logging:
  level:
    org:
      hibernate:
        type:
          descriptor:
            sql: trace

springdoc:
  swagger-ui:
    path: /swagger-ui.html
    groups-order: DESC
    operationsSorter: method
    disable-swagger-default-url: true
    display-request-duration: true
  api-docs:
    path: /api-docs
  show-actuator: true
  default-consumes-media-type: application/json
  default-produces-media-type: application/json
  paths-to-match:
    - /v1/**